# Core
ultralytics>=8.3
opencv-python>=4.10
numpy>=1.26
pandas>=2.2
scipy>=1.13
matplotlib>=3.8
tqdm>=4.66
pillow>=10.0

# Tracking (ByteTrack matcher)
lap>=0.5

# PyTorch — install the build that matches your CUDA toolkit.
# CPU-only:   pip install torch
# GPU (CUDA): see https://pytorch.org/get-started/locally/
torch>=2.2
